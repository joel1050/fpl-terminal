# Optional: enables the server-side DeepSeek analyst.
DEEPSEEK_API_KEY=
# Optional; defaults to the model used by the local MVP.
DEEPSEEK_MODEL=deepseek-v4-flash
# Optional local diagnostics; keep false for normal use.
NEXT_PUBLIC_DEBUG_AI=false
